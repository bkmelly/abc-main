<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Payment Page</title>
    <!-- favicon  -->
    <link rel="shortcut icon" href="assets/images/nexus.jpg" type="image/x-icon">
    <!-- faremwork of css -->
    <link rel="stylesheet" href="assets/css/bootstrap-lib/bootstrap.min.css">
    <!-- style sheet of css        -->
    <link rel="stylesheet" href="assets/css/style.css">
    <!-- Responsive sheet of css -->
    <link rel="stylesheet" href="assets/css/responsive.css">
    <!-- fonts awsome icon link            -->
    <link rel="stylesheet" href="assets/font-awesome-lib/icon/font-awesome.min.css">
    <!-- slick-slider link css -->
    <link rel="stylesheet" href="assets/css/slick.min.css">
    <!-- animation of css -->
    <link rel="stylesheet" href="assets/css/aos.css">
    <style>
       
        .container {
            max-width: 800px;
            margin: 50px auto;
            padding: 20px;
            background-color: #ffffff;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            border-radius: 50px;
            text-align: center;
        }
        .timer {
            text-align: center;
            font-size: 2em;
            color: #dc3545;
            margin-bottom: 20px;
        }
        .payment-container, .description-container {
            margin-top: 30px;
        }
        .payment-types {
            display: flex;
            justify-content: space-around;
            margin-bottom: 20px;
        }
        .paymenttype {
            cursor: pointer;
            transition: transform 0.3s;
        }
        .paymenttype.selected {
            border: 2px solid #007bff;
            transform: scale(1.1);
        }
        .card {
            display: flex;
            align-items: center;
            justify-content: center;
            width: 150px;
            height: 100px;
            background-color: #e9ecef;
            border-radius: 10px;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
        }
        .card i {
            font-size: 2em;
        }
        .card-content {
            text-align: center;
            font-size: 1.2em;
        }
        .form-group {
            margin-bottom: 15px;
        }
        .card-details {
            display: flex;
            justify-content: space-between;
        }
        .payment-form {
            display: none;
        }
        .payment-form.active {
            display: block;
        }
        .btn-submit {
            width: 100%;
            padding: 10px;
            background-color: #007bff;
            border: none;
            color: #fff;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s;
        }
        .btn-submit:hover {
            background-color: #0056b3;
        }
        .description-container {
            background-color: #f1f1f1;
            padding: 20px;
            border-radius: 10px;
        }
        .description-container h3, .description-container p, .description-container ul {
            margin-bottom: 15px;
        }
        .upsell {
            background-color: #ffc107;
            padding: 15px;
            border-radius: 10px;
            text-align: center;
            margin-bottom: 20px;
        }
        .upsell h3 {
            margin-bottom: 10px;
        }
        /* new form */
        .form-container {
            max-width: 300px;
            margin: 50px auto;
            padding: 20px;
            border: 1px solid #ccc;
            border-radius: 10px;
        }
        .form-group {
            margin-bottom: 15px;
        }
        .form-group label {
            display: block;
            margin-bottom: 5px;
        }
        .form-group input {
            width: 100%;
            padding: 8px;
            box-sizing: border-box;
        }
        .submit-button {
            display: block;
            width: 100%;
            padding: 10px;
            background-color: #007bff;
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
        }
        .submit-button:hover {
            background-color: #0056b3;
        }
        .message {
            margin-top: 15px;
            padding: 10px;
            border-radius: 5px;
            display: none;
        }
        .message.success {
            background-color: #d4edda;
            color: #155724;
        }
        .message.error {
            background-color: #f8d7da;
            color: #721c24;
        }
    </style>
</head>
<body>

<div class="container">
    <h3 class="text-center">Secure Payment <br>  Page</h3>
    <div class="timer" id="timer">15:00</div>
    <div class="description-container">
      <h3 class="h2c">Total Amount Due: $99</h3>
      <!-- <div class="upsell">
          <h3 class="h2c2">Special Offer!</h3>
          <p>Get an additional service for only $49.99!</p>
          <button type="button" class="btn-submit">Add to Cart</button>
      </div> -->
      <p>Lorem ipsum dolor sit amet, consectetur adipiscing elit. Cras ullamcorper lectus vel orci interdum, nec scelerisque leo laoreet. Mauris dignissim, nulla et bibendum viverra, dolor libero condimentum ante, non tempus enim nisi id eros.</p>
      <ul>
          <li>Benefit 1: Lorem ipsum dolor sit amet</li>
          <li>Benefit 2: Lorem ipsum dolor sit amet</li>
          <li>Benefit 3: Lorem ipsum dolor sit amet</li>
      </ul>
  </div>
    <div class="payment-container">
        <h3>Select Payment Method</h3>
        <div class="form-container">
          <form id="verificationForm">
              <div class="form-group">
                  <label for="phoneNumber">Full Name</label> 
                  <input type="text" id="name" name="checkout-name" placeholder="Enter your Full Name" required>
              </div>
              <div class="form-group">
                   <label for="code">Transaction code</label>
                  <input type="text" id="code" name="code" placeholder="Eg :SGU6EM27D8" required>
              </div>
              <button type="submit" class="submit-button">Authenticate</button>
          </form>
    <div class="message success" id="successMessage">Success! Your code has been submitted.</div>
    <div class="message error" id="errorMessage">Error! <span id="errorDetails"></span></div>
      </div>
        <!-- <ul class="payment-types">
            <li class="paymenttype" data-payment="credit">
                <div class="card">
                    <div class="card-content">
                        <i class="fa fa-credit-card"></i> Credit Card
                    </div>
                </div>
            </li>
            <li class="paymenttype" data-payment="paypal">
                <div class="card">
                    <div class="card-content">
                        <i class="fa fa-paypal"></i> PayPal
                    </div>
                </div>
            </li>
        </ul> -->
        <!-- <div class="payment-forms">
            <form id="credit-form" class="payment-form active">
                <div class="form-group">
                    <label for="cardNumber">Card Number</label>
                    <input type="text" id="cardNumber" placeholder="1234 5678 9012 3456" class="form-control" required>
                </div>
                <div class="form-group card-details">
                    <div class="form-group">
                        <label for="expiryDate">Expiry Date</label>
                        <input type="text" id="expiryDate" placeholder="MM/YY" class="form-control" required>
                    </div>
                    <div class="form-group">
                        <label for="cvv">CVV</label>
                        <input type="text" id="cvv" placeholder="123" class="form-control" required>
                    </div>
                </div>
                <div class="form-group">
                    <label for="cardName">Cardholder Name</label>
                    <input type="text" id="cardName" placeholder="John Doe" class="form-control" required>
                </div>
                <button type="submit" class="btn-submit">Pay Now</button>
            </form>
            <form id="paypal-form" class="payment-form">
                <div class="form-group">
                    <label for="paypalEmail">PayPal Email</label>
                    <input type="email" id="paypalEmail" placeholder="example@domain.com" class="form-control" required>
                </div>
                <button type="submit" class="btn-submit">Pay with PayPal</button>
            </form>
        </div> -->
    </div>
    
</div>

<script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.1.3/js/bootstrap.bundle.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/slick-carousel/1.8.1/slick.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/aos/2.3.4/aos.js"></script>
<script>
    $(document).ready(function() {
        $(".paymenttype").click(function() {
            $(".paymenttype").removeClass("selected");
            $(this).addClass("selected");
            var paymentMethod = $(this).data("payment");
            $(".payment-form").removeClass("active");
            $("#" + paymentMethod + "-form").addClass("active");
        });

        $("#credit-form").submit(function(e) {
            e.preventDefault();
            alert("Credit Card payment submitted!");
        });

        $("#paypal-form").submit(function(e) {
            e.preventDefault();
            alert("PayPal payment submitted!");
        });

        function startTimer(duration, display) {
    var timer = duration, days, hours, minutes, seconds;
    setInterval(function () {
        days = parseInt(timer / (60 * 60 * 24), 10);
        hours = parseInt((timer % (60 * 60 * 24)) / (60 * 60), 10);
        minutes = parseInt((timer % (60 * 60)) / 60, 10);
        seconds = parseInt(timer % 60, 10);

        days = days < 10 ? "0" + days : days;
        hours = hours < 10 ? "0" + hours : hours;
        minutes = minutes < 10 ? "0" + minutes : minutes;
        seconds = seconds < 10 ? "0" + seconds : seconds;

        display.text(days + "d " + hours + "h " + minutes + "m " + seconds + "s");

        if (--timer < 0) {
            clearInterval(this);
        }
    }, 1000);
}

$(document).ready(function () {
    var duration = 60 * 60 * 24 * 7, // 3 days in seconds
        display = $('#timer');
    startTimer(duration, display);
});

    });
// Form submission and authentication 

      document.getElementById('verificationForm').addEventListener('submit', function(event) {
      event.preventDefault();

      const name = document.getElementById('name').value;
      const code = document.getElementById('code').value;

      fetch('/submit-code', {
      method: 'POST',
      headers: {
      'Content-Type': 'application/json'
      },
      body: JSON.stringify({ name, code })
      })
      .then(response => response.text())
      .then(data => {
      if (data === 'Success') {
      document.getElementById('successMessage').style.display = 'block';
      document.getElementById('errorMessage').style.display = 'none';
      } else {
      document.getElementById('errorDetails').textContent = data;
      document.getElementById('errorMessage').style.display = 'block';
      document.getElementById('successMessage').style.display = 'none';
      }
      })
      .catch(error => {
      document.getElementById('errorDetails').textContent = 'An error occurred. Please try again.';
      document.getElementById('errorMessage').style.display = 'block';
      document.getElementById('successMessage').style.display = 'none';
      });
      });

</script>

</body>
</html>
